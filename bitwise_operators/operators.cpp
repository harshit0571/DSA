#include <iostream>
#include <vector>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    // and operators (&)
    // int x = 1 & 0;
    // int y = 20 & 1;
    // cout << y;

    // or operators (||)
    // int x = 1 || 1;
    // int y = 20 || 0;
    // cout << x;

    // ex-or (^)
    // int x = 411 ^ 414;
    // cout << x;

    // left shift operator(<<)
    // int x = 10 << 1;
    // cout << x;

    // right Shift operator
    // float x = 11 >> 1;
    // cout << x;
}